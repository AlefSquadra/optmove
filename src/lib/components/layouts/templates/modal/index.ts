import { ModalBody, ModalFooter, ModalHeader, ModalRoot } from "./Modal";

export const Modal = {
  Header: ModalHeader,
  Body: ModalBody,
  Footer: ModalFooter,
  Root: ModalRoot,
};

export type { IModalProps, ModalBodyProps, ModalFooterProps, ModalHeaderProps } from "./Modal.types";
