import { Radio, type RadioProps, RadioGroup, type RadioGroupProps } from "@fluentui/react-components";

const OptRadio = (props: RadioProps) => {
  return <Radio {...props} />;
};

const OptRadioGroup = (props: RadioGroupProps) => {
  return <RadioGroup {...props} />;
};

export { OptRadio, OptRadioGroup };
