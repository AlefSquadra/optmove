import { Tab, type TabProps, TabList, type TabListProps } from "@fluentui/react-components";

const OptTab = (props: TabProps) => {
  return <Tab {...props} />;
};

const OptTabList = (props: TabListProps) => {
  return <TabList {...props} />;
};

export { OptTab, OptTabList };
